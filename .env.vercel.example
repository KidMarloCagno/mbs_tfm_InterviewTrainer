# Copy these values into Vercel Project Settings -> Environment Variables
# Do not commit real secrets.
#
# Scope guidance:
# - Set DB vars and auth secrets for Development, Preview, and Production.
# - Set NEXTAUTH_URL for Production only.
#   (Do not set NEXTAUTH_URL for Preview to avoid callback-host mismatches.)

POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""
NEXTAUTH_SECRET=""
AUTH_SECRET=""
NEXTAUTH_URL=""
